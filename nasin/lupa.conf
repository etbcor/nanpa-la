# http block (redirect to https)
server {
    listen 80;      # IPv4
    listen [::]:80; # IPv6
    server_name nanpa.la ~^(?:[\w-]+\.)+nanpa\.la$;

    # http -> https
    location / { return 301 https://$server_name$request_uri; }
}

